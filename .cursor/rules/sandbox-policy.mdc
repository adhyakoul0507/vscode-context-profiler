---
description: "AI Agent Sandbox Policy — auto-generated by Context Profiler"
alwaysApply: true
---

# 🛡️ Security Sandbox Policy

You are operating under a **strict least-privilege security policy** for this project.
This policy was auto-generated by [Context Profiler](https://github.com/adhyakoul0507/vscode-context-profiler) using an offline LLM analysis of the project requirements.

**Reasoning:** No reasoning provided.

---

## ✅ Allowed Terminal Commands

You may ONLY run commands that begin with the following prefixes:

- `npm`
- `npx`
- `git`

## 🚫 Blocked Terminal Commands

You must NEVER run any of the following:

- `rm -rf`
- `sudo`
- `chmod 777`
- `curl | sh`
- `wget | sh`

If a command is not in the allowed list, you MUST stop and ask the user for explicit approval before executing it.

---

## 🌐 Allowed Network Domains

You may only make network requests to:

- `registry.npmjs.org`
- `github.com`

All other outbound network access is **blocked by policy**. Do not attempt to reach any other domain.

---

## 📁 File Access

### Allowed Paths
- `/home/user/project/./`
- `/home/user/project/src/`
- `/home/user/project/public/`

### Blocked Paths (NEVER access these)
- `/home/user/.ssh`
- `/home/user/.aws`
- `/home/user/.gnupg`
- `/etc`
- `~/.ssh`
- `~/.aws`
- `~/.gnupg`

---

## ⚠️ General Rules

1. **Default Deny**: If you are unsure whether an action is permitted, **do not do it**. Ask the user.
2. **No Escalation**: Never attempt privilege escalation (`sudo`, `su`, `doas`).
3. **No Exfiltration**: Never archive or transmit files outside the project directory.
4. **No Sensitive Reads**: Never read SSH keys, AWS credentials, GPG keys, or `.env` files outside the project.
5. **Audit Trail**: When executing any terminal command, clearly state what you are running and why.
